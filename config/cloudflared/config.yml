# cloudflared tunnel configuration
# Managed by: mac-mini-server repo
#
# The credentials file is NOT committed to the repo.

tunnel: e4978b52-8394-4f5b-b715-ee96a5a9e641
credentials-file: /Users/ben/.cloudflared/e4978b52-8394-4f5b-b715-ee96a5a9e641.json

ingress:
  # Route all traffic to Caddy reverse proxy
  # Caddy handles per-subdomain routing via its Caddyfile
  - service: http://localhost:80
